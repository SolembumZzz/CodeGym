let value = 10;
let numbers = [-3, 5, 1, 3, 2, 10];
for (let i = 0; i < numbers.length; i++) {
    if (value == numbers[i]) {
        console.log(`Value ${numbers[i]} is found at index ${i}`);
    }
}